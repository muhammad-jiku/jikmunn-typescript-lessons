var userId;
var username;
var userVerified;
userId = 15232;
username = "shang-chi";
userVerified = true;
console.log("".concat(username.toLowerCase(), " is now verified by userId no.").concat(userId, ", which is ").concat(userVerified, "."));
