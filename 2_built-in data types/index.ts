let userId: number;
let username: string;
let userVerified: boolean;

userId = 15232;
username = "shang-chi";
userVerified = true;

console.log(
  `${username.toLowerCase()} is now verified by userId no.${userId}, which is ${userVerified}.`
);
